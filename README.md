# electronics_code
The code of electronics project
the first file (ele.ino) is the code of the master controller.
the second file (slaver) is the code of slaver controller which controls the movement of the system.
